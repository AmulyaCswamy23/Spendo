Django>=4.2
gunicorn>=21.2.0                 # For production server if deployed
django-allauth>=0.60.0           # For Google OAuth login
python-decouple>=3.8             # For .env file management
dj-database-url>=2.1.0           # Useful if deploying on Heroku
whitenoise>=6.6.0                # To serve static files
Pillow>=10.3.0                   # For image uploads (if needed)
speechrecognition>=3.10.0       # Voice input (if used in backend)
pyaudio>=0.2.13                  # Required for mic input (may be optional)
